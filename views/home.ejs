<% layout('layouts/boilerplate')%>
<%- include('partials/flash') %>
        <div class="banner">
            <nav class="navbar navbar-expand-lg navbar-light" id="guestNavbar">
                <div class="container-fluid">
                  <a class="navbar-brand guestBrand" href="/"><img src="/icons/logo3.png"></a>
                  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                  </button>
                  <div class="collapse navbar-collapse" id="navbarText">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="guestNav">
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/deathintimation">Death Intimation</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard/aboutus">About Us</a>
                        </li>
                        </ul>
                    </div>
                </div>
            </nav>
            <div class="intro">
                <h1>HOOGHLY TREASURY ONE</h1>
                <p>We are delighted to serve you! Now apply for different pension related services online. <br> Thank You for being with us.</p>
                <div>
                    <% if(!currentUser){%>
                    <button type="submit" onclick="location.href='/register'"> <span></span>SIGN UP</button>
                    <button type="submit" onclick="location.href='/login'"> <span></span>LOG IN</button>
                    <%}else{%>
                        <%if(currentUser.role ===2){%>
                            <button type="submit" onclick="location.href='/admin-dashboard'"> <span></span>VISIT</button>
                        <%}%>
                        <%if(currentUser.role ===0){%>
                            <button type="submit" onclick="location.href='/dashboard'"> <span></span>VISIT</button>
                        <%}%>
                    <%}%>
                </div>
            </div>
        </div>

